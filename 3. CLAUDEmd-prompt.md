 Generate a minimal CLAUDE.md for this project. Before writing anything, read: package.json,
  tsconfig.json (if present), and scan the top-level directory structure and files in /docs.                                
                                                                         
  Only include what Claude cannot derive from reading the code. Do not document things that are          
  self-evident from file structure, package.json scripts, or standard framework conventions.             
   
  Include only these sections if they contain non-obvious information:                                   
                                                                  
  Commands — build, test, lint, dev server. Only include if the command is non-standard (e.g. pnpm       
  db:migrate is worth including; npm run build is not — Claude already knows to check scripts).
                                                                                                         
  Project structure — only if the layout deviates from framework defaults (e.g. src/ instead of app root,
   monorepo workspace names, non-standard config locations).
                                                                                                         
  Critical constraints — rules Claude would violate by default. Examples: "never use db push, always use 
  migrations", "all API routes require auth middleware", "state management is Zustand not Context". Do
  not include generic best practices.                                                                    
                                                                  
  Non-obvious architecture — only decisions that affect how Claude should write new code and aren't      
  visible from existing code patterns. One sentence each.
                                                                                                         
  Do not include:                                                 
  - Project description or background
  - Technology list (Claude can see imports)                                                             
  - File path inventories                   
  - Anything already in README.md                                                                        
  - Generic rules ("write clean code", "follow best practices")   
  - Anything Claude can discover by reading 2-3 files                                                    
                                                                                                         
  Target: under 40 lines. Every line must answer "would Claude get this wrong without being told?" — if  
  not, cut it.                                                                                           
                                                                                                         
  Write the file to CLAUDE.md at the project root.  
