Generate a minimal CLAUDE.md for this project.

## Before writing, read:
- package.json and tsconfig.json (if present)
- Top-level directory structure
- Files in /docs (if present)
- Any existing README.md (to avoid duplicating it)

## What to include

Only include sections that contain non-obvious information. Every line must answer: "would Claude get this wrong without being told?"

### Commands
Only non-standard commands. If the command matches the framework default or is obvious from package.json scripts, skip it. Include things like custom migration commands, workspace-specific test commands, or multi-step workflows.

### Project structure
Only if the layout deviates from framework defaults (e.g. monorepo workspace names, non-standard config locations, shared package conventions).

### Critical constraints
Rules Claude would violate by default. Examples:
- Migration vs push policy (e.g. "never use db push, always use migrations")
- Auth requirements (e.g. "all API routes require auth middleware")
- State management choice if non-obvious from imports
- Environment-specific rules (e.g. "never commit .env, use .env.example as reference")
- Branching or commit conventions if enforced

### Code quality rules
Only rules that go beyond standard practice and that Claude would not infer from the codebase:
- Max file length (e.g. "keep files under 250 lines; split only when it improves clarity")
- Service layer pattern (e.g. "no direct DB calls from routes or components")
- Where to put shared types, constants, and utilities
- Error handling pattern (e.g. "use Result types, not thrown exceptions" or "all API errors return {error, code} shape")
- Import conventions (e.g. "use path aliases, no relative imports deeper than 2 levels")
- Component patterns (e.g. "server components by default, 'use client' only when needed")
- Test expectations (e.g. "every new service file needs a corresponding test file")

### Non-obvious architecture
Only decisions that affect how Claude writes new code and aren't visible from existing patterns. One sentence each.

## What to exclude
- Project description or background
- Technology list (Claude can see imports)
- File path inventories
- Anything already in README.md
- Generic rules ("write clean code", "follow best practices", "use meaningful variable names")
- Anything Claude can discover by reading 2-3 existing files
- Package-specific version notes unless there's a known incompatibility

## Format rules
- Target: under 50 lines
- Use terse language — sentence fragments are fine
- Group related rules under minimal headings
- No preamble or explanation — just the rules

Write the file to CLAUDE.md at the project root.
